#include<bits/stdc++.h>
#include"baseOfLinkedList.cpp"
using namespace std;

void reverse(Node *& head)
{
    Node * curr=head;
    Node* prev = NULL,*Next = head;

    while(Next->next!=NULL)
    {
       


    }
}

int main()
{

    return 0;
}